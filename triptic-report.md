# Triptic 코드 분석 보고서

**대상 저장소:** `lkjh7609-sketch/kunyang` (main @ `3abc219`)
**분석 파일:** `index.html` 1개 파일 · 2,743줄 · 157 KB
**프레임워크:** 없음 (Vanilla JS, 빌드 도구 없이 단일 HTML)
**외부 API:** Google Maps · Firebase Realtime Database · aviationstack
**확인된 결함:** 18건 / **개선 제안:** 기능 24건, 디자인 22건

Triptic은 여행 일정·항공편·숙소·식사·경비를 한 화면에서 관리하는 지도 기반 플래너입니다.

---

## 목차

1. [요약](#01-요약)
2. [기술 스택](#02-기술-스택)
3. [데이터 구조](#03-데이터-구조)
4. [데이터 흐름](#04-데이터-흐름)
5. [구현된 기능](#05-구현된-기능)
6. [확인된 결함](#06-확인된-결함)
7. [사용성 개선 제안](#07-사용성-개선-제안)
8. [UI·UX와 디자인](#08-uiux와-디자인)
9. [실행 순서](#09-실행-순서)

---

## 01. 요약

개인 프로젝트치고 완성도가 상당히 높습니다. 다만 "만든 사람만 아는 기능"이 많고, 눈에 띄지 않게 죽어 있는 코드가 있으며, 여행 앱의 가장 중요한 순간인 *현지에서 인터넷이 끊겼을 때*를 대비하지 못하고 있습니다.

**잘 되어 있는 것.** 숙소 순연 로직(하루만 입력하면 이후 날짜에 자동 적용), 거리 기반 구역 자동 분리(오사카→교토 이동일에 지도가 국가 단위로 줌아웃되지 않게 처리), 항공편 자동조회 실패 시 수동 입력 대체 경로, 홈 화면 아이콘 재설치로 데이터가 날아가는 iOS 문제를 URL에 복구 키를 심어 우회한 것 — 전부 실제로 써 보면서 부딪힌 문제를 해결한 흔적입니다. 주석의 밀도와 설명 수준도 좋습니다.

**가장 급한 것.**
1. PWA 매니페스트 연결이 잘못된 태그로 작성돼 Android에서 앱 설치가 동작하지 않습니다.
2. 여행 이름·날짜·도시·항공편을 *생성 후 수정할 방법이 화면에 없습니다* — 수정 모달 코드는 70줄 넘게 완성돼 있지만 이를 호출하는 버튼이 HTML에서 누락됐습니다.
3. 장소 이름과 메모를 이스케이프 없이 화면에 그리고 있어, 공유 링크를 통해 다른 사람 브라우저에서 스크립트가 실행될 수 있습니다.

**가장 큰 기회.** 이미 Directions API로 구간별 대중교통 경로를 계산하고 있으면서, 그 결과에서 *소요 시간*을 꺼내 쓰지 않고 있습니다. 여기에 체류 시간 개념만 더하면 "시간을 하나하나 손으로 고치는 앱"에서 "시간이 자동으로 맞춰지는 앱"으로 성격이 바뀝니다. 투자 대비 효과가 가장 큰 한 수입니다.

---

## 02. 기술 스택

빌드 단계가 전혀 없는 구성입니다. `index.html` 하나를 정적 호스팅에 올리면 그대로 동작하고, 나머지는 전부 런타임에 CDN과 API에서 가져옵니다.

| 계층 | 사용 기술 | 비고 |
|---|---|---|
| 구조 | HTML5 단일 파일 (2,743줄) | CSS 약 200줄 · JS 약 2,420줄이 모두 인라인 |
| 스타일 | 순수 CSS, 클래스 기반 | CSS 변수 미사용 — 색·간격이 전부 하드코딩 |
| 로직 | Vanilla JS (ES2020) | 전역 함수 + 전역 변수 방식, 모듈·번들러 없음 |
| 지도 | Google Maps JavaScript API | Places(자동완성·상세정보), Directions(대중교통), Marker |
| 드래그 | SortableJS `@latest` | `forceFallback:true`로 터치 안정성 확보 |
| PDF | jsPDF 2.5.1 (UMD) | 텍스트 직접 렌더링 + 나눔고딕 TTF 런타임 주입 |
| 압축 | lz-string 1.5.0 | 구버전 `?share=` 링크 해독 전용 (레거시) |
| 항공편 | aviationstack REST | `/flights` + `/airports` 2회 호출 |
| 서버 | Firebase Realtime Database | SDK 없이 `.json` REST만 사용 · asia-southeast1 |
| 로컬 저장 | localStorage | `smartPlannerAllProjects` 외 2개 키 |
| 앱화 | manifest.json + apple-touch-icon | 서비스 워커 없음 → 오프라인 불가 |

### 이 선택의 장단점

단일 파일 구조는 배포가 압도적으로 간단하고, 파일 하나만 백업하면 전체가 보존된다는 실질적 이점이 있습니다. 개인이 혼자 관리하는 규모에서는 합리적인 선택입니다.

대신 2,700줄을 넘어서면서 한계가 드러나기 시작합니다. 전역 변수 `plannerData`, `hotelsData`, `mealsData`, `expensesData`, `flightsData`, `currentDay`가 서로 얽혀 있고, 상태를 바꾸는 쪽에서 `saveData(); renderList(); updateMarkers(); refreshExtraButtonStates();`를 매번 손으로 나열해야 합니다. 이 네 줄은 코드 전체에 10번 넘게 반복되며, 하나라도 빠뜨리면 화면과 데이터가 어긋납니다. 실제로 렌더링 함수와 마크업이 어긋난 결함이 이미 발생해 있습니다(F-02).

> 파일을 쪼개지 않더라도, 상태 변경을 `commit(변경내용)` 같은 함수 하나로 모으고 그 안에서 저장·렌더를 호출하게 바꾸면 이 종류의 버그는 구조적으로 사라집니다. 리팩터링 비용은 반나절 수준입니다.

---

## 03. 데이터 구조

모든 여행 데이터는 `allProjects` 객체 하나에 들어가고, 이것이 localStorage와 Firebase 양쪽에 그대로 직렬화됩니다.

```js
allProjects = {
  "오사카 3박4일 가족여행": {          // ← 여행 이름이 곧 기본키
    city, cityLat, cityLng,            // 목적지 (Places에서 선택)
    startDate, endDate,                // "YYYY-MM-DD"
    data:     { 1:[place,…], 2:[…] },  // 일차별 방문 장소 (순서 = 일정 순서)
    hotels:   { 1:{name,address,lat,lng} },
    meals:    { 1:{ breakfast:{…,skip}, lunch:{…}, dinner:{…} } },
    expenses: { 1:[{desc, amount}] },
    flights:  { outbound:flight, return:flight },
    currentDay, shareId, lastModified
  }
}

place  = { name, address, lat, lng, placeId?, time:"HH:MM", memo, mealType? }
flight = { flightNo, date, airline?, manual?,
           dep:{iata,name,lat,lng,time}, arr:{…} }
```

### 구조에서 읽히는 설계 의도와 부작용

- **일차(1,2,3…)가 데이터의 축입니다.** 날짜가 아니라 순번이 키이므로, 여행 기간을 앞으로 당기면 "1일차에 있던 내용"이 새 날짜에 그대로 따라옵니다. 의도한 동작이라면 괜찮지만 사용자에게는 설명이 필요합니다.
- **여행 이름이 기본키입니다.** 그래서 이름 변경 기능이 없고, 같은 이름을 두 번 만들 수 없으며, 이름에 작은따옴표가 들어가면 삭제 버튼이 깨집니다(F-03). 내부 ID를 두고 이름은 속성으로 내리는 편이 안전합니다.
- **식사는 두 곳에 중복 저장됩니다.** `meals`에 원본이 있고, `syncMealItemsIntoList()`가 이를 `data[day]`에도 복사해 넣습니다. 이 동기화 과정에서 해당 날짜 배열 전체가 시간순으로 재정렬되기 때문에, 드래그로 맞춰둔 순서가 식사를 추가하는 순간 초기화됩니다(F-08).
- **경비에 통화 필드가 없습니다.** 금액은 숫자 하나뿐이고 화면과 PDF 모두 "원"으로 고정 출력합니다. 해외여행 앱으로서 가장 큰 데이터 모델상의 공백입니다.
- **장소에 체류 시간이 없습니다.** 시작 시각만 있고 소요 시간이 없어, 일정이 겹치는지 앱이 판단할 근거가 없습니다.

---

## 04. 데이터 흐름

저장은 localStorage가 정본이고, Firebase는 두 가지 역할(기기 백업 · 공유 스냅샷)로 그림자처럼 따라붙습니다. 사용자는 이 동작을 거의 인지하지 못합니다.

### 편집자(파티장) 쪽 흐름

```
입력 (장소 추가 · 시간 변경 · 메모 · 숙소 · 식사 · 경비)
   │ 즉시
   ▼
saveData()
   전역 상태 → allProjects → localStorage.setItem
   (동기 저장. 깨진 항목(좌표 없는 데이터)을 여기서 걸러냄)
   │ 디바운스
   ├─ 1.5초 후 → PUT /devicebackups/{backupId}   (전체 프로젝트 목록을 통째로 덮어씀)
   ├─ 1.2초 후 → PUT /shares/{shareId}           (공유 링크를 만든 적 있을 때만)
   └─ 요청 시 → Google / aviationstack           (자동완성 · 장소 상세 · 경로 · 항공편)
```

### 앱을 열 때의 분기

```
initSystem() — URL 쿼리를 먼저 확인
   │
   ├─ ?id=xxxxxxx   → 게스트 뷰
   │                  shares에서 스냅샷을 읽어 읽기 전용 화면 진입. 로비를 거치지 않음
   ├─ ?share=…      → 레거시 링크
   │                  LZString으로 URL에 통째로 압축돼 있던 구버전 공유 방식
   └─ 그 외          → 내 로비
                      복구 키를 URL에 심고, 로컬이 비었으면 클라우드 백업에서 복원
```

### 게스트 ↔ 파티장 동기화

- 게스트 화면은 **5초마다** `GET /shares/{id}`를 폴링하고, 직전 응답 문자열과 다르면 전체를 다시 그립니다. 탭이 백그라운드여도 멈추지 않습니다.
- 게스트가 장소를 건의하면 `PUT /suggestions/{shareId}/{제안ID}`로 즉시 올라갑니다. 이름은 브라우저에 기억해 두었다가 다음에 다시 묻지 않습니다.
- 파티장은 **"받은 제안 확인하기" 버튼을 직접 누를 때만** 제안 목록을 가져옵니다. 제안이 도착했다는 사실을 알려주는 장치가 없습니다.
- 제안을 수락하면 해당 일차 배열 끝에 추가되고, 서버의 제안 항목은 `DELETE`로 제거됩니다. 메모에 `[제안 · 민수]` 형태로 출처가 남는 처리가 좋습니다.

### Firebase에 실제로 쌓이는 경로

| 경로 | 내용 | 키 길이 | 지우는 방법 |
|---|---|---|---|
| `/devicebackups/{id}` | 그 기기의 *모든* 여행 데이터 | 10자 | 없음 |
| `/shares/{id}` | 공유 중인 여행 1건의 전체 스냅샷 | 7자 | 없음 |
| `/suggestions/{shareId}/{id}` | 게스트가 보낸 장소 제안 | 7자 | 수락·무시 시 자동 삭제 |

> 프로젝트를 삭제해도 localStorage에서만 지워집니다. 서버의 백업·공유 스냅샷은 그대로 남고, 공유 링크를 가진 사람은 삭제된 여행을 계속 볼 수 있습니다(F-06).

---

## 05. 구현된 기능

코드 기준으로 실제 동작하는 기능을 모두 정리했습니다. 괄호 안은 그 기능에서 특히 잘 만들어진 부분입니다.

### 여행 관리
- 여러 여행 프로젝트를 만들고 목록에서 선택 · 삭제
- 목적지 도시를 Google Places 자동완성으로 선택 (좌표까지 저장해 지도 초기 중심으로 사용)
- 출발일·종료일로 일차 탭 자동 생성, 각 탭에 실제 날짜 병기
- 전체 데이터를 JSON 파일로 내보내기 / 가져오기 (이름 충돌 시 덮어쓸 대상을 미리 알려줌)

### 일정 편집
- 장소 검색 후 일차별 목록에 추가, 드래그로 순서 변경
- 드래그가 어려운 환경을 위한 **▲▼ 버튼 대체 수단** (세심한 배려)
- 장소별 시·분 선택(5분 단위)과 자유 메모
- 카드를 누르면 평점·리뷰 수·영업시간·전화·웹사이트·사진을 모달로 표시 (구버전 데이터는 좌표로 `place_id`를 역추적한 뒤 저장해 다음부터 빠르게 조회)
- 편집 모드 / 뷰 모드 전환

### 숙소 · 식사 · 경비
- 일차별 숙소 지정. 입력하지 않은 날은 **직전 숙소를 이어서 사용**하는 것으로 자동 처리
- 하루의 시작(전날 숙소)과 끝(그날 숙소)을 구분해 표시. 첫날은 출발 숙소 없음, 마지막 날은 복귀 숙소 없음 — 실제 여행 흐름과 정확히 일치
- 아침·점심·저녁 각각 식당 검색, "건너뛰기" 체크, 기본 시각(08:00 / 12:30 / 18:30)으로 일정에 자동 삽입
- 일차별 경비 입력과 그날 합계 · 전체 여행 합계
- 숙소·식사·경비 버튼에 데이터 유무를 테두리 색으로 표시

### 항공편
- 편명 + 날짜로 aviationstack 자동조회 → 출발·도착 시각과 공항 좌표까지 수집
- 조회 실패를 전제한 **수동 입력 경로**: 항공사·공항·이착륙 시각 직접 입력, 공항명은 Places 자동완성으로 좌표까지 확보
- 공항 이름을 자동완성 선택 후 다시 손으로 고치면 좌표를 폐기하는 처리 (이름과 좌표가 어긋나는 것을 방지)
- 첫날 맨 위에 도착편, 마지막 날 맨 아래에 귀국편 카드 자동 배치
- 공항 좌표는 지도 범위 계산에서 **의도적으로 제외** (넣으면 지도가 대륙 단위로 줌아웃되는 문제를 미리 차단)

### 지도
- 그날의 장소를 번호 마커로, 숙소를 민트색 원형 마커로 표시하고 전체가 보이게 자동 맞춤
- **거리 기반 구역 자동 분리**: 인접 장소 간 직선거리가 20km를 넘으면 다른 도시로 판단해 구역을 나누고, 이동일에는 마지막 구역만 지도에 표시
- 뷰 모드에서 인접 구간을 대중교통 경로선으로 연결, 경로를 못 찾으면 점선 직선으로 대체
- 카드 사이마다 구글 맵스 대중교통 길찾기 딥링크 (항공편·숙소·장소를 하나의 순서로 엮어 항상 "두 지점 사이"에 링크가 놓이도록 설계)

### 공유 · 협업
- 보기 전용 공유 링크 생성. 한 번 만든 링크는 계속 유효하며, 이후 편집 내용이 자동 반영
- 공유받은 사람은 로비 없이 바로 일정만 보고, 5초 폴링으로 최신 상태 유지
- 게스트의 장소 건의 → 파티장이 확인 후 원하는 일차에 반영
- 게스트 이름을 기기에 기억

### 출력 · 저장
- 현재 일차를 텍스트 기반 PDF로 내보내기. 나눔고딕을 CDN에서 받아 내장해 한글 깨짐 없음, 내용이 넘치면 자동 페이지 분할, 페이지 번호 표기
- 별도 볼드 폰트 없이 같은 글자를 0.15mm 겹쳐 그려 굵게 보이게 하는 처리 (파일 크기를 아끼는 영리한 절충)
- 홈 화면 아이콘 재설치 시 데이터 유실을 막는 URL 내장 복구 키 + 클라우드 자동 복원
- 스플래시 화면, 구글 지도 인증 실패 시 원인 체크리스트 안내 모달

---

## 06. 확인된 결함

코드를 직접 확인해 재현 근거가 있는 것만 담았습니다. 위에서부터 고치는 순서대로 정렬했습니다.

### F-01 · 치명 — PWA 매니페스트가 연결되지 않습니다
9번째 줄이 `<meta name="manifest" content="manifest.json">`으로 되어 있습니다. 브라우저는 이 태그를 무시합니다. 매니페스트는 `link` 태그로만 인식됩니다.

결과적으로 Android Chrome에서 설치 배너가 뜨지 않고, 설치하더라도 앱 이름·아이콘·전체화면 모드가 적용되지 않습니다. iOS는 `apple-mobile-web-app-*` 메타 태그로 별도 동작하기 때문에 iPhone에서만 테스트했다면 증상을 발견할 수 없었을 겁니다. 잘 만들어 둔 `manifest.json`이 지금까지 한 번도 읽히지 않았습니다.

**수정:** `<link rel="manifest" href="manifest.json">` 한 줄로 교체. 1분.

### F-02 · 치명 — 여행 정보를 수정할 방법이 화면에 없습니다
`openEditTripModal()`(664행)은 도시·기간·양방향 항공편을 모두 고칠 수 있는 완성된 함수지만, **코드 어디에서도 호출되지 않습니다.** 이 함수를 여는 `.trip-edit-btn`과, 도시·기간을 요약해 보여주는 `.trip-info-bar` 영역이 CSS에는 정의돼 있지만 `<body>` 마크업에서 통째로 빠져 있습니다.

연쇄적으로 `renderTripInfoBar()`(1407행)도 `#trip-info-text`를 찾지 못해 매번 아무 일도 하지 않고 종료합니다. 이 함수는 날짜 변경·공유 갱신 등 4곳에서 호출되고 있어, 겉보기엔 정상 동작처럼 보입니다.

사용자 입장에서는 **여행을 만든 뒤 날짜를 하루 늘리거나, 항공편을 추가하거나, 목적지를 바꾸는 것이 영구히 불가능합니다.** 유일한 방법은 프로젝트를 지우고 처음부터 다시 만드는 것입니다.

**수정:** 사이드바 `.mode-toggle` 위에 요약 바 마크업을 추가하고 버튼에 `onclick="openEditTripModal()"`를 연결. 로직은 이미 전부 있으므로 마크업 8줄이면 끝납니다.

### F-03 · 치명 — 사용자 입력을 이스케이프 없이 화면에 그립니다
`escapeHtml()` 함수가 정의돼 있지만 제안 목록에서만 쓰입니다. 정작 `renderLobby()`의 여행 이름, `renderList()`의 장소 이름·주소·**메모**는 `innerHTML`에 그대로 들어갑니다.

메모는 사용자가 자유롭게 쓰는 값이고, 공유 링크를 통해 *다른 사람의 브라우저에서* 렌더링됩니다. 여기에 스크립트를 심으면 그 사람의 저장 데이터를 읽거나 공유 스냅샷을 조작할 수 있습니다. 지인끼리 쓰는 앱이라 악의적 시나리오는 낮지만, 구조적으로 열려 있는 구멍입니다.

같은 원인의 즉각적인 버그도 있습니다. 삭제 버튼이 `onclick="deleteProject('${name}')"` 형태라, 여행 이름에 작은따옴표가 들어가면(예: `Ben's trip`) JS 문자열이 깨져 **삭제 버튼이 조용히 동작을 멈춥니다.**

**수정:** 모든 사용자 입력 출력에 `escapeHtml()` 적용. 인라인 `onclick`에 이름을 넣는 대신 `dataset` + `addEventListener`로 전환. 반나절.

### F-04 · 치명 — 서버 데이터가 사실상 무방비입니다
Firebase를 인증 없이 REST로만 쓰고 있으므로, 규칙이 공개 읽기·쓰기로 열려 있어야만 동작합니다. 즉 **ID만 알면 누구나 해당 경로를 읽고, 덮어쓰고, 지울 수 있습니다.**

공유 ID는 7자(약 3.5조 가지)라 무작위 대입은 현실적이지 않습니다. 진짜 문제는 **기기 복구 키가 주소창에 노출된다**는 점입니다. `?backup=`이 URL에 항상 붙어 있고, 사용자는 그 의미를 모릅니다. 스크린샷을 찍거나, 링크를 그대로 복사해 보내거나, 화면을 공유하는 순간 상대는 그 사람의 *모든 여행 데이터*를 읽고 덮어쓸 수 있는 키를 갖게 됩니다.

aviationstack 키도 소스에 평문으로 들어 있어 누구나 그대로 가져다 쓸 수 있습니다. Google Maps 키는 리퍼러 제한으로 방어가 가능하지만, 설정돼 있는지 확인이 필요합니다.

**수정:** ① 복구 키를 URL에서 빼고 "복구 코드" 카드로 로비에 명시적으로 노출(사용자가 그 존재와 성격을 알게 함). ② Firebase 규칙에서 `devicebackups`는 쓰기만 허용하고 읽기는 키 검증을 거치게. ③ aviationstack 호출을 Cloud Functions 같은 프록시 뒤로. ④ Google 키에 HTTP 리퍼러 제한 적용.

### F-05 · 높음 — 오프라인에서 앱이 열리지 않습니다
서비스 워커가 없어 `index.html`조차 캐시되지 않습니다. 해외에서 데이터 로밍이 끊기거나 지하철·건물 안에서 신호가 약하면 앱 자체가 뜨지 않습니다. 여행 앱이 가장 필요한 순간이 바로 그때입니다.

구글 지도 타일은 어차피 네트워크가 필요하지만, **일정 텍스트·시간·주소·메모는 전부 localStorage에 이미 있습니다.** 앱 셸만 캐시해도 "지도는 안 뜨지만 오늘 일정은 다 보인다" 수준은 즉시 달성됩니다.

**수정:** 앱 셸(HTML·CSS·아이콘)을 캐시하는 서비스 워커 30줄. 지도 영역은 오프라인일 때 "지도를 불러올 수 없습니다 · 주소 복사" 대체 화면으로.

### F-06 · 높음 — 프로젝트를 지워도 공유 링크가 살아 있습니다
`deleteProject()`는 `allProjects`에서만 삭제합니다. `/shares/{id}`와 `/devicebackups`는 그대로 남습니다. 링크를 받은 사람은 삭제된 여행의 마지막 스냅샷을 계속 폴링하며 보게 됩니다. 공유를 중단하는 방법 자체가 앱에 없습니다.

**수정:** 삭제 시 `DELETE /shares/{shareId}`와 `/suggestions/{shareId}`를 함께 호출. 별도로 "공유 중단" 버튼을 공유 모달 안에 추가.

### F-07 · 높음 — 항공편 자동조회가 대부분 실패합니다
코드 주석도 이미 인지하고 있는 문제입니다. aviationstack 무료 플랜은 미래 날짜 조회가 제한되고 HTTPS를 지원하지 않는 경우가 많아, HTTPS로 서빙되는 이 앱에서는 요청 자체가 차단될 수 있습니다. 여행 계획은 정의상 미래 날짜이므로 **자동조회 성공률이 구조적으로 낮습니다.**

더 큰 문제는 실패 처리 방식입니다. 실패할 때마다 `alert()`가 뜨고, 저장 버튼을 누를 때 한 번 더 시도하면서 팝업이 또 뜹니다. 사용자는 "고장난 기능"으로 인식합니다.

**수정:** 수동 입력을 *기본*으로 승격하고 자동조회를 보조 버튼으로 내리기. 실패 시 팝업 대신 입력란 아래에 회색 한 줄로 "자동조회 결과 없음 — 아래에 직접 입력해 주세요"를 띄우고 수동 필드를 자동으로 펼치기.

### F-08 · 높음 — 식사를 추가하면 드래그로 맞춘 순서가 초기화됩니다
`syncMealItemsIntoList()`가 마지막에 해당 일차 배열 **전체**를 시간순으로 정렬합니다. 시간 순서와 다르게 의도적으로 배치해 둔 일정(예: 예약 시각과 실제 동선이 다른 경우)이 식사 항목을 넣거나 "건너뛰기"를 체크하는 순간 전부 재정렬됩니다.

더 놀라운 건 `exportToPDF()`가 시작하자마자 이 함수를 호출한다는 점입니다. **PDF를 내보내는 것만으로 화면의 일정 순서가 바뀝니다.** 사용자는 원인을 짐작할 수 없습니다.

**수정:** 전체 정렬 대신 식사 항목만 시간에 맞는 위치에 삽입. PDF 내보내기에서는 동기화 호출을 제거하거나, 정렬하지 않는 읽기 전용 버전을 사용.

### F-09 · 높음 — 여행 기간을 줄이면 데이터가 유령으로 남습니다
`generateSchedule()`은 일수가 줄어도 `plannerData[5]` 같은 잔여 일차를 삭제하지 않습니다. 탭에서 사라질 뿐 데이터는 남아 있고, 나중에 기간을 다시 늘리면 옛 일정이 되살아납니다. 경비 합계는 화면에 보이지 않는 날짜까지 포함해 계산되므로 **총액이 실제보다 크게 나옵니다.**

**수정:** 기간 축소 시 "N일차 이후 일정 M건이 삭제됩니다"라고 확인받고 실제로 제거. 또는 보관 후 복구 가능하게.

### F-10 · 중간 — 저장 실패를 감지하지 못합니다
`localStorage.setItem()`에 `try/catch`가 없습니다. 용량 초과(보통 5MB)나 사파리 프라이빗 모드에서 예외가 던져지면 그 시점의 호출 스택 전체가 중단되고, 사용자는 **아무 안내 없이 편집 내용을 잃습니다.** 장소마다 주소·좌표·메모가 쌓이고 사진 첨부 같은 기능이 추가되면 한도에 도달할 수 있습니다.

**수정:** `try/catch`로 감싸고 실패 시 "저장 공간이 부족합니다 — 지난 여행을 내보내기 후 삭제해 주세요" 안내. 중장기적으로는 IndexedDB로 이전.

### F-11 · 중간 — 지도 API 호출이 낭비되고 있습니다
뷰 모드로 전환하거나 날짜 탭을 누를 때마다 인접 구간 수만큼 `DirectionsService`를 새로 호출합니다. 10개 장소가 있는 날이면 한 번 볼 때마다 9건, 5일치를 훑으면 수십 건입니다. 캐시가 없어 같은 구간을 반복 조회합니다.

Directions는 유료 과금 대상입니다. 개인 사용 수준에서는 무료 크레딧 안에 들어오겠지만, 사용자가 늘면 비용이 선형으로 증가합니다.

**수정:** 출발·도착 좌표를 키로 결과를 메모이즈하고, 좌표가 바뀌지 않았으면 재호출하지 않기. 같은 작업으로 **소요 시간도 함께 확보**되므로 U-02로 바로 이어집니다.

### F-12 · 중간 — 게스트 폴링이 계속 돕니다
5초 간격 `setInterval`이 탭 가시성과 무관하게 무한히 실행됩니다. 공유 링크를 열어두고 다른 앱을 쓰는 동안에도 배터리와 데이터를 소모합니다. 해외 로밍 중이라면 체감되는 비용입니다.

**수정:** `visibilitychange`에서 폴링 정지·재개. 화면이 돌아올 때 즉시 1회 갱신. 간격을 15초로 늘려도 체감 차이는 거의 없습니다.

### F-13 · 중간 — 확대를 막아 두었습니다
`maximum-scale=1.0, user-scalable=no`는 접근성 기준(WCAG 1.4.4) 위반이며, 작은 글씨가 많은 이 앱에서는 실사용에도 불편합니다. 주소와 영업시간은 12px, 빈 상태 문구는 더 작습니다. iOS는 이 설정을 무시하지만 Android는 실제로 확대를 차단합니다.

**수정:** `content="width=device-width, initial-scale=1"`로 축소. 입력 확대가 걱정된다면 입력란 폰트를 16px 이상으로 유지하면 iOS 자동 확대가 발생하지 않습니다(이미 검색창은 16px).

### F-14 · 중간 — 라이브러리 버전이 고정돼 있지 않습니다
`sortablejs@latest`는 라이브러리가 새 버전을 내는 순간 예고 없이 바뀝니다. 어제 되던 드래그가 오늘 안 될 수 있고, 여행 중에 그 일이 일어나면 손쓸 방법이 없습니다. CDN이 침해당했을 때의 위험도 함께 짊어집니다.

**수정:** `sortablejs@1.15.2`처럼 버전 고정 + 세 라이브러리 모두에 `integrity`(SRI) 해시 추가.

### F-15 · 낮음 — 수정 시각이 날짜까지만 기록됩니다
`toLocaleDateString()`이라 시·분이 없습니다. 같은 날 여러 번 편집해도 구분이 안 되고, 로비 목록을 최근 순으로 정렬할 근거도 없습니다. 현재 목록은 **생성 순서**로 고정돼 있어 여행이 쌓일수록 최근 것을 찾기 어려워집니다.

**수정:** 타임스탬프(`Date.now()`)를 함께 저장하고, 로비 기본 정렬을 "출발일 임박 순 → 최근 수정 순"으로.

### F-16 · 낮음 — 두 기기에서 동시에 고치면 한쪽이 사라집니다
클라우드 백업은 전체를 통째로 덮어쓰는 방식(`PUT`)이고, 복원은 "로컬에 없는 프로젝트만 채우기"입니다. 폰과 노트북에서 *같은* 여행을 각각 편집하면 나중에 저장한 쪽이 상대를 덮어씁니다. 경고도 병합도 없습니다.

**수정:** 프로젝트별 `updatedAt`을 비교해 서버가 더 최신이면 "다른 기기에서 수정된 내용이 있습니다 — 불러오기 / 내 것 유지"를 묻기.

### F-17 · 낮음 — PDF가 하루치만 나오고, 게스트는 받을 수 없습니다
버튼 문구는 "일정표 PDF 내보내기"지만 실제로는 **현재 보고 있는 일차**만 출력됩니다. 4박5일이면 다섯 번 눌러 다섯 개 파일을 만들어야 합니다. 게다가 버튼이 `view-only` 클래스라 편집 모드에서는 보이지 않고, 공유받은 게스트 화면에서는 CSS로 아예 숨겨져 있습니다. 오프라인 대비가 가장 필요한 사람이 게스트인데 저장 수단이 없습니다.

**수정:** "이 날짜만 / 전체 일정" 선택 제공, 게스트 화면에서도 허용, 버튼 문구를 실제 동작에 맞추기.

### F-18 · 낮음 — 장소 삭제에 확인도 되돌리기도 없습니다
프로젝트 삭제에는 `confirm()`이 있지만, 장소 삭제 버튼은 한 번 누르면 즉시 사라집니다. 메모를 길게 적어둔 장소도 마찬가지이고, 지도 마커 옆 좁은 화면에서 오탭이 나기 쉽습니다.

**수정:** 삭제 후 "삭제했습니다 · 되돌리기" 스낵바를 5초간 표시. 확인 팝업보다 흐름이 끊기지 않으면서 더 안전합니다.

---

## 07. 사용성 개선 제안

"기능을 더 넣자"가 아니라 "지금 사용자가 손으로 하고 있는 일을 앱이 대신하자"는 방향으로 정리했습니다.

### 일정을 짜는 경험

**U-01 · 장소에 체류 시간을 추가하기**
지금은 각 장소에 시작 시각만 있습니다. 여기에 "얼마나 머무를까"가 더해지면 앱이 하루를 계산할 수 있게 됩니다. 기본값을 카테고리별로 제안하면(식당 60분, 박물관 90분, 전망대 45분) 입력 부담도 거의 없습니다.

**U-02 · 이동 시간을 표시하고 시간을 자동으로 밀어주기**
이 앱은 이미 구간마다 대중교통 경로를 계산하고 있습니다. 응답에 들어 있는 `duration`을 꺼내기만 하면 됩니다. 카드 사이 딥링크 옆에 "🚇 22분"을 붙이고, 앞 장소의 시간을 바꾸면 뒤 일정이 함께 밀리게 하세요. **이 변화 하나가 앱의 성격을 바꿉니다.** 지금은 "적어두는 곳"이고, 그 뒤엔 "짜주는 곳"이 됩니다.

**U-03 · 무리한 일정을 미리 경고하기**
U-01·U-02가 있으면 계산이 나옵니다. "10:00 출발인데 이동만 50분 — 도착하면 이미 11:00입니다" 같은 안내를 카드 아래 한 줄로. 별도 화면 없이 인라인으로 두는 편이 낫습니다.

**U-04 · 영업시간과 충돌 알려주기**
장소 상세 모달에서 이미 `opening_hours`를 받아오고 있습니다. 이걸 저장해 두면 "화요일 휴무인데 화요일 일정에 들어 있습니다"를 알려줄 수 있습니다. 여행에서 실제로 가장 자주 발생하는 사고입니다.

**U-05 · 장소를 다른 날로 옮기기**
현재는 불가능합니다. 3일차를 2일차로 옮기려면 지우고 다시 검색해야 하고, 메모도 함께 사라집니다. 일정 조정은 여행 계획에서 가장 자주 하는 동작입니다. 카드 메뉴에 "다른 날로 이동"만 넣어도 큰 차이가 납니다.

**U-06 · 장소 추가를 한 동작으로**
지금은 검색 → 목록에서 선택 → "추가" 버튼까지 세 번입니다. 자동완성에서 선택하는 순간 추가되도록 하고, 잘못 넣었으면 되돌리기로 회수하는 편이 자연스럽습니다.

**U-07 · 구글 지도 링크 붙여넣기**
여행 계획은 보통 카톡으로 링크를 주고받는 데서 시작합니다. 클립보드의 구글 지도 URL을 붙여넣으면 장소로 변환되게 하면 실제 사용 흐름과 맞아떨어집니다.

**U-08 · 후보 목록(장바구니)**
"가고 싶은데 언제 갈지는 아직" 상태의 장소를 담아둘 곳이 없습니다. 날짜에 배정하지 않은 후보 서랍을 두고, 거기서 날짜로 끌어다 놓게 하면 계획 초기 단계가 훨씬 편해집니다.

### 경비

**U-09 · 통화와 환율**
해외여행 앱인데 금액 단위가 "원"으로 고정돼 있습니다. 여행마다 현지 통화를 정하고, 입력은 현지 통화로, 합계는 두 통화를 나란히 보여주세요. 환율은 여행 시작 시 한 번 받아 저장해두면 오프라인에서도 계산됩니다.

**U-10 · 나누어 내기**
가족·친구 여행에서 경비 기록의 실제 목적은 "총 얼마 썼나"가 아니라 "누가 누구에게 얼마를 줘야 하나"입니다. 참여자를 등록하고 항목마다 "누가 냈는지"만 표시해도 정산이 자동으로 나옵니다. 공유·제안 기능이 이미 있으니 참여자 개념과 자연스럽게 이어집니다.

**U-11 · 항목을 장소와 묶기**
경비는 별도 서랍에 따로 있습니다. 장소 카드에서 바로 금액을 적을 수 있게 하면 입력 시점이 실제 지출 시점과 일치합니다. 나중에 "이 식당에서 얼마 썼더라"를 찾기도 쉬워집니다.

**U-12 · 예산과 분류**
전체 예산을 정하고 소진율을 보여주기, 식비·교통·입장료·쇼핑으로 분류해 일자별 추이 보기. 여행 후 회고에도 쓰입니다.

### 같이 짜는 경험

**U-13 · 제안이 왔다는 걸 알려주기**
지금은 파티장이 버튼을 눌러봐야 압니다. 편집 화면 진입 시 제안 개수를 조회해 버튼에 배지(`2`)를 붙이세요. 이미 폴링 코드가 있으니 같은 방식으로 붙이면 됩니다.

**U-14 · 권한 단계 나누기**
현재는 "보기 전용" 하나뿐입니다. 보기 / 제안 가능 / 함께 편집 세 단계로 나누면, 배우자에게는 편집 권한을 주고 친구들에게는 제안만 받는 식의 실제 여행 구성과 맞출 수 있습니다.

**U-15 · 공유 중단과 만료**
한 번 만든 링크를 없앨 방법이 없습니다(F-06). "공유 중단" 버튼과 "여행 종료 후 자동 만료" 옵션을 공유 모달에 넣으세요.

**U-16 · 투표**
후보가 여럿일 때 제안에 좋아요를 누를 수 있게 하면, 파티장이 혼자 결정하는 부담이 줄고 참여감이 생깁니다. 데이터 구조는 제안 객체에 카운터 하나만 추가하면 됩니다.

### 가지고 다니는 경험

**U-17 · 오프라인 우선**
F-05의 서비스 워커가 전제입니다. 그 위에 "지도 없이 일정만" 모드를 두고, 각 장소에 좌표 기반 오프라인 딥링크와 "주소 복사" 버튼을 붙이면 현지 택시 기사에게 보여주는 용도로 그대로 쓰입니다.

**U-18 · 오늘 화면**
여행 중에는 5일치 탭이 필요 없습니다. 오늘 날짜가 여행 기간 안이면 앱을 열자마자 오늘 일차로 이동하고, "지금 다음 일정"을 맨 위에 고정하세요. 계획하는 앱과 여행하는 앱은 필요한 화면이 다릅니다.

**U-19 · 캘린더로 내보내기**
`.ics` 파일 생성은 텍스트 조립이라 라이브러리도 필요 없습니다. 시간·장소·메모가 이미 있으므로 곧바로 만들 수 있고, 사용자의 기본 캘린더 앱에서 알림까지 받을 수 있습니다.

**U-20 · 텍스트로 복사**
실제로 사람들은 일정을 카톡에 붙여넣어 공유합니다. "일정 텍스트로 복사" 버튼 하나가 PDF보다 더 자주 쓰일 가능성이 높습니다.

### 데이터 안심

**U-21 · 복구 코드를 드러내기**
URL에 몰래 심어둔 방식은 영리하지만, 사용자가 모르면 지켜줄 수도 없습니다. 로비에 "이 기기의 복구 코드: `KJ7F-2M9A` · 다른 기기에서 불러오기" 카드를 두면, 동작 원리가 드러나면서 기기 간 이동도 훨씬 쉬워집니다. 보안 문제(F-04)도 같이 해결됩니다.

**U-22 · 여행 복제**
"작년 오사카 일정을 그대로 가져와 날짜만 바꾸기"는 재방문자에게 가장 가치 있는 기능입니다. 구현은 객체 복사와 이름 변경뿐입니다.

**U-23 · 첫 화면에서 막히지 않게**
지금 빈 로비에는 "저장된 여행이 없습니다"만 있습니다. 예시 여행(오사카 3박4일)을 한 번에 불러오는 버튼을 두면, 사용자가 만들기 전에 무엇을 만들 수 있는지 먼저 보게 됩니다.

**U-24 · 사진과 예약 정보**
숙소 예약 번호, 항공권 QR, 식당 예약 확인 화면 — 여행자가 실제로 찾아 헤매는 것들입니다. 장소·숙소·항공편에 이미지와 짧은 메모를 붙일 수 있게 하면 앱이 "여행 서류함"이 됩니다. 다만 localStorage 용량 문제(F-10)를 먼저 해결해야 합니다.

---

## 08. UI·UX와 디자인

전반적으로 2018년경 유행한 플랫 UI 스타일입니다. 나쁘지 않지만 색과 규칙이 흩어져 있어 "정돈되지 않은" 인상을 줍니다. 아래는 지금 상태를 수치로 확인한 뒤 정리한 조언입니다.

| 항목 | 현재 | 권장 |
|---|---|---|
| 고유 색상 | 31개 | 주색 1 · 보조 1 · 의미색 3 · 중성 5 |
| 글자 크기 | 15단계 (10~24px) | 5단계 (12 / 14 / 16 / 20 / 28) |
| 모서리 반경 | 10종 (4~26px) | 3종 (6 / 12 / 999) |
| 패딩 값 | 13종 (2~22px) | 4의 배수 6단계 |
| alert / confirm | 39회 | 인라인 검증 + 스낵바 |
| CSS 변수 | 0개 | 토큰화 필수 |

### 구조와 흐름

**D-01 · 사이드바 상단이 버튼으로 막혀 있습니다**
"프로젝트 목록으로", "공유 링크 만들기", "받은 제안 확인하기" 세 개의 전폭 버튼이 세로로 쌓여 있고, 그 아래 모드 전환 탭까지 더하면 **첫 화면의 절반이 도구로 채워집니다.** 정작 일정은 스크롤해야 보입니다.

이 셋을 한 줄짜리 상단 바로 압축하세요. 왼쪽에 뒤로가기와 여행 이름, 오른쪽에 공유 아이콘과 더보기(⋯) 메뉴. 세로 공간을 120px 이상 돌려받습니다.

**D-02 · 편집 모드와 뷰 모드를 다시 생각해보세요**
모드 개념은 사용자가 배워야 하는 비용입니다. 게다가 두 모드의 차이가 일관되지 않습니다 — 경로선은 뷰 모드에서만 그려지고, PDF 버튼도 뷰 모드에만 있고, 길찾기 딥링크도 뷰 모드 전용입니다. "보기 모드가 오히려 기능이 더 많은" 상태입니다.

대안은 모드를 없애고 항상 편집 가능하게 두되, 편집 요소(드래그 핸들·시간 선택·삭제)를 카드를 탭했을 때만 펼치는 것입니다. 모드가 꼭 필요하다면 이름을 "편집 / 여행 중"으로 바꿔 *언제 쓰는지*를 드러내세요.

**D-03 · 지도 비율이 고정입니다**
모바일에서 지도 35% / 목록 65%로 고정돼 있습니다. 동선을 확인하고 싶을 땐 지도가 너무 작고, 메모를 쓸 땐 지도가 자리를 뺏습니다. 목록을 바텀시트로 만들어 위아래로 끌어 3단계(지도 위주 / 반반 / 목록 전체)로 스냅되게 하면 한 화면에서 두 용도가 모두 해결됩니다.

**D-04 · 날짜 탭에 정보를 더 담으세요**
현재는 "3일차 (8/12)"가 전부입니다. 요일을 함께 보여주고(휴무일 판단에 필요), 장소가 없는 날은 흐리게, 오늘 날짜는 점으로 표시하세요. 일차가 7개를 넘으면 가로 스크롤 속에서 현재 위치를 잃기 쉬우니 선택된 탭이 항상 보이도록 자동 스크롤도 필요합니다.

### 시각 언어

**D-05 · 액센트 색을 줄이세요**
보라(`#6c5ce7`), 파랑(`#0984e3`), 민트(`#00cec9`), 노랑(`#fdcb6e`), 주황(`#e17055`), 초록(`#00b894`)이 동시에 쓰입니다. 색이 많으면 **어떤 색도 의미를 갖지 못합니다.** 사용자는 "파란 버튼이 주요 동작"이라는 규칙을 배울 수 없습니다.

주색 하나(파랑)를 정해 주요 동작에만 쓰고, 보라는 현재 선택 상태 표시로만, 나머지는 성공·경고·위험 세 가지 의미색으로 정리하세요.

**D-06 · 삭제 버튼이 위험해 보이지 않습니다**
노란 배경(`#ffeaa7`)에 빨간 글씨(`#d63031`) 조합은 경고인지 위험인지 읽히지 않고, 노란색 때문에 오히려 눈에 잘 띕니다. 삭제는 평소엔 조용해야 합니다 — 회색 텍스트 버튼이나 더보기 메뉴 안쪽에 두고, 누른 뒤의 확인 단계에서만 빨강을 쓰세요.

**D-07 · 본문 대비가 기준 미달입니다**
보조 텍스트 `#7f8fa6`는 흰 배경 대비 약 3.3:1, 빈 상태 문구 `#b2bec3`는 약 2.0:1입니다. 작은 글씨 기준 4.5:1에 못 미칩니다. 햇빛 아래에서 폰을 보는 상황이 많은 여행 앱에서는 실사용 문제이기도 합니다. `#5C6E80` 수준까지 어둡게 하세요.

**D-08 · 점선 테두리를 줄이세요**
공유 버튼, 제안 버튼, 경로 링크, 숙소·식사·경비 토글이 전부 `dashed`입니다. 점선은 시각적으로 "임시" 또는 "비활성"을 뜻하는데, 정작 활성 버튼들에 쓰이고 있습니다. 실선이나 옅은 배경 채움으로 바꾸세요.

**D-09 · 이모지를 아이콘 대신 쓰지 마세요**
✈️ 🏨 🍽 💰 🚇 📄가 기능 아이콘 역할을 하고 있습니다. 운영체제마다 모양·색·크기·정렬 기준이 달라 통제가 불가능하고, 일부 안드로이드에서는 흑백으로 나옵니다. 라인 아이콘 세트 하나(Lucide 등)로 통일하고, 이모지는 정말 감성이 필요한 한두 곳에만 남기세요.

**D-10 · 그림자를 계층 표현에 쓰세요**
지금은 거의 모든 카드가 같은 `0 2px 5px rgba(0,0,0,0.05)`를 씁니다. 전부 같은 높이로 떠 있으면 계층이 전달되지 않고 화면만 뿌예집니다. 카드는 1px 보더로, 그림자는 실제로 떠 있는 것(모달·바텀시트·드래그 중인 항목)에만.

**D-11 · 한글 폰트를 지정하세요**
현재 폰트 스택에 한글 폰트가 없어, 기기마다 애플 SD 산돌고딕 / 노토 산스 / 맑은 고딕으로 제각각 렌더링됩니다. 자간과 줄높이 설계가 무의미해집니다. Pretendard나 IBM Plex Sans KR을 지정하고 `word-break: keep-all`을 함께 적용하세요(한글 단어가 중간에서 잘리는 것을 막아줍니다).

**D-12 · 값을 토큰으로 묶으세요**
색 하나를 바꾸려면 지금은 24곳을 찾아 고쳐야 합니다. `:root`에 CSS 변수로 색·간격·반경·그림자를 정의하면 위 D-05부터 D-10까지가 한 번에 정리되고, 다크 모드(D-13)도 거의 공짜로 따라옵니다. **디자인 개선 중 투자 대비 효과가 가장 큰 항목입니다.**

**D-13 · 다크 모드**
비행기 안, 야간 이동, 숙소 침대 — 이 앱이 쓰이는 상황의 상당수가 어두운 곳입니다. 토큰화가 되어 있으면 `prefers-color-scheme` 블록 하나로 대응됩니다. 구글 지도도 다크 스타일을 지원합니다.

### 상호작용

**D-14 · 터치 영역이 너무 작습니다**
순서 변경 ▲▼ 버튼이 24×18px입니다. 권장 최소치는 44×44px이고, 이 버튼은 *드래그가 어려운 사람을 위한 대체 수단*인데 오히려 더 정밀한 조작을 요구합니다. 버튼 자체는 작게 두더라도 투명한 터치 영역을 44px로 넓히세요. 접이식 섹션의 ✕ 닫기 버튼도 같은 문제입니다.

**D-15 · 시스템 팝업을 걷어내세요**
`alert`·`confirm`이 39회 쓰입니다. 시스템 대화상자는 브랜드가 없고, 흐름을 끊고, PWA에서는 도메인 이름까지 노출되어 특히 거칠게 느껴집니다.
- 입력 검증("여행 이름을 입력해주세요") → 해당 입력란 아래 빨간 한 줄
- 완료 알림("링크가 복사되었습니다") → 하단 스낵바 2초
- 파괴적 확인("정말 삭제하시겠습니까") → 인앱 모달, 또는 아예 "되돌리기" 스낵바로 대체

**D-16 · 빈 화면과 실패 화면을 설계하세요**
빈 로비는 한 문장뿐이고, 일정이 없는 날은 완전한 백지입니다. 공유 링크로 들어갔을 때 데이터를 불러오는 동안에도 빈 화면이 보입니다.

빈 화면은 다음 행동을 안내할 기회입니다. "이 날은 아직 비어 있어요 — 가고 싶은 곳을 검색해 추가해 보세요" 한 줄과 검색창으로의 시선 유도만 있어도 충분합니다. 로딩에는 스켈레톤을, 실패에는 *무엇이 잘못됐고 무엇을 하면 되는지*를 쓰세요. 구글 지도 인증 오류 모달은 이미 이 원칙을 잘 지키고 있습니다.

**D-17 · 키보드로 닿지 않는 요소들**
접이식 섹션의 `.close-x`, 장소 카드의 `.place-info-trigger`, 항공편 `.flight-manual-toggle`, 로비의 프로젝트 카드가 모두 `div`/`span` + `onclick`입니다. 탭 키로 도달할 수 없고 스크린 리더가 버튼으로 읽지 않습니다. `<button>`으로 바꾸면 해결됩니다. 아이콘만 있는 버튼에는 `aria-label`을 붙이세요.

**D-18 · 모달의 기본기**
Esc로 닫기, 배경 클릭으로 닫기(장소 상세 모달에만 있음), 열릴 때 첫 입력란 포커스, 포커스 가두기, 배경 스크롤 잠금 — 다섯 가지가 대부분의 모달에 없습니다. 모달 생성 함수를 하나로 통일하면서 한 번에 넣으세요.

**D-19 · 스플래시를 짧게**
매 실행마다 1.8초입니다. 처음엔 좋지만 여행 중에 하루 열 번 열면 18초를 버립니다. 첫 실행에만 보여주거나 0.6초로 줄이세요.

**D-20 · 데스크톱을 진짜로 쓰세요**
PC에서 로비가 480px 모바일 틀 그대로 가운데 떠 있습니다. 넓은 화면에서는 여행 카드를 그리드로 펼치고, 계획 단계에서 실제로 유용한 것(여러 날짜를 나란히 보기, 지도를 크게 두고 검색 결과를 옆에)에 공간을 쓰세요. 계획은 주로 PC에서, 실행은 폰에서 — 두 화면의 역할이 다릅니다.

**D-21 · 진행 상황을 보여주세요**
"5일 중 3일 계획 완료" 같은 표시를 로비 카드에 넣으면 다시 열 이유가 생깁니다. 여행 준비는 여러 날에 걸쳐 조금씩 하는 일이라, 어디까지 했는지를 기억해 주는 것 자체가 기능입니다.

**D-22 · 이름을 붙이는 방식을 일관되게**
"프로젝트 목록으로 돌아가기", "여행 이름", "새 여행 만들기", "파티장" — 같은 대상을 프로젝트·여행으로 번갈아 부르고 있습니다. 사용자 화면에서는 "여행"으로 통일하고 "프로젝트"는 코드에만 남기세요. 마찬가지로 공유하는 사람은 "파티장"보다 "만든 사람"이 이해하기 쉽습니다.

---

## 09. 실행 순서

효과 대비 비용 순으로 묶었습니다. 1단계는 대부분 몇 줄 수정이고, 2단계까지 끝내면 남에게 권할 수 있는 앱이 됩니다.

### 1단계 — 지금 바로 (하루 이내 · 대부분 몇 줄)
- **F-01** 매니페스트 `link` 태그로 교체 — 1분
- **F-02** 여행 정보 요약 바 + 수정 버튼 마크업 추가 — 30분 (로직은 이미 완성)
- **F-13** 확대 차단 해제 — 1분
- **F-14** 라이브러리 버전 고정 + SRI — 10분
- **F-08** 식사 동기화의 전체 정렬 제거 — 20분
- **F-10** 저장 실패 `try/catch`와 안내 — 20분
- **F-12** 폴링을 탭 가시성에 연동 — 15분

### 2단계 — 이번 주 (신뢰할 수 있는 앱 만들기)
- **F-03** 전 구간 이스케이프 처리, 인라인 `onclick` 제거
- **F-04** 복구 키를 URL에서 빼고 **U-21** 복구 코드 카드로 전환, Firebase 규칙 정비
- **F-05** 서비스 워커로 오프라인 진입 보장
- **F-06 · F-09** 삭제·기간 축소 시 데이터 정리
- **F-18 · D-15** alert/confirm을 스낵바와 인라인 검증으로 교체하면서 되돌리기 도입
- **D-12** CSS 토큰화 — 이후 모든 디자인 작업의 전제

### 3단계 — 이번 달 (앱의 성격을 바꾸는 작업)
- **U-01 · U-02 · U-03** 체류 시간 + 이동 시간 + 일정 자동 조정 ⭐ 핵심
- **F-11** 경로 결과 캐시 (U-02와 같은 코드에서 처리)
- **U-05 · U-06 · U-08** 날짜 간 이동, 한 번에 추가, 후보 서랍
- **U-09 · U-10** 통화와 정산
- **D-01 · D-02 · D-03** 화면 구조 개편 (상단 바 · 모드 재정의 · 바텀시트)
- **D-05 ~ D-11** 색·타이포·아이콘 정리, 다크 모드

### 4단계 — 그 다음 (있으면 좋은 것)
- **U-13 ~ U-16** 협업 강화 (알림 배지 · 권한 · 투표)
- **U-18 · U-19 · U-20** 여행 중 화면, 캘린더·텍스트 내보내기
- **U-22 · U-24** 여행 복제, 사진·예약 정보 첨부
- **F-16** 기기 간 충돌 감지
- **D-20 · D-21** 데스크톱 레이아웃, 진행률 표시

---

마지막으로 한 가지. 이 코드에서 가장 인상적인 부분은 기능의 수가 아니라 **실패를 전제한 설계**입니다. 항공편 조회가 안 될 걸 알고 수동 입력을 준비했고, 드래그가 안 될 수 있으니 버튼을 뒀고, 경로를 못 찾으면 점선을 그리고, 홈 화면 아이콘이 데이터를 날릴 걸 알고 URL에 키를 심었습니다. 위에 적은 개선안들도 같은 태도로 접근하면 됩니다 — 이동 시간이 계산 안 되는 구간, 영업시간 정보가 없는 장소, 오프라인 상태를 먼저 정하고 나머지를 만드는 식으로요.

---

*분석 대상: `lkjh7609-sketch/kunyang` · `index.html` 2,743줄 · 커밋 `3abc219`*
*코드를 직접 읽고 작성했으며, 결함 항목은 모두 해당 코드 위치를 확인했습니다. 실제 브라우저 실행 테스트는 포함되지 않았습니다.*
